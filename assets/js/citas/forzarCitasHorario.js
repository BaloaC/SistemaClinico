export default function forzarCitasHorario(input){

    console.log("aa")

    // if(input.value === "true" && input.dataset.modaltarget === "registro"){

    //     console.log("a")
    //     document.querySelector(".hora_entrada_registro").disabled = false;
    // } else {
    //     document.querySelector(".hora_entrada_registro").disabled = true;

    // }
}

window.forzarCitasHorario = forzarCitasHorario;